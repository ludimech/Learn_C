#include <stdio.h>

int main()
{
    //declaring variables - telling the computer that we need space in memory to store our data
    int x = 6;
    int y = 9;

    printf("The number stored in x is: %d\n", x);
    printf("The number stored in y is: %d\n", y);

    //This function in the program allows us to understand if all functions are wowrking or not
    //If yes, print 0, if no, print 1.
    return 0;
}